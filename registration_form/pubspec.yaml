name: registration_form
description: Three-screen Flutter registration form (Home → Page 1 → Page 2).
publish_to: "none"
version: 0.0.0

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
